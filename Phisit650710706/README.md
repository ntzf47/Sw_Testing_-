#  Software Testing นายพิสิษฐ์ สุประดิษฐ์ 650710706

## (Manual Test Cases)

| Test Case ID | โมดูล / ฟังก์ชัน | ประเภท | คำอธิบาย | ขั้นตอนการทดสอบ | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|---|---|
| TC-PEN-001 | Login | Negative | ลองใส่ SQL Injection ตอนล็อกอิน | 1. ไปหน้า Login<br>2. ใส่ Username: `admin' OR '1'='1`<br>3. ใส่รหัสอะไรก็ได้<br>4. กดเข้า | ระบบไม่ให้เข้า และแจ้งว่าข้อมูลไม่ถูกต้อง | N/A |
| TC-PEN-002 | Login | Negative | เดารหัสผ่านซ้ำ ๆ (Brute force) | ยิง request ใส่รหัสผิดหลายครั้งติดกัน | ระบบควรล็อกบัญชีชั่วคราว | N/A |
| TC-PEN-003 | Register | Negative | ใส่ script ใน email (XSS) | ใส่ `<script>alert(1)</script>@test.com` แล้วสมัคร | ระบบต้องไม่รับ และแจ้งว่า email ไม่ถูกต้อง | N/A |
| TC-PEN-004 | Register | Positive | สมัครสมาชิกปกติ | กรอกข้อมูลครบ แล้วกดยืนยัน | สมัครสำเร็จ และไปหน้า login | N/A |
| TC-PEN-005 | Session | Negative | ใช้ token เดิมหลัง logout | logout แล้วเอา token เดิมไปยิง request | ระบบต้องไม่ให้ใช้ และขึ้น unauthorized | N/A |
| TC-PEN-006 | Admin Area | Negative | ข้ามสิทธิ์เข้าหน้าจัดการแบบไม่ Login | เข้า URL `/admin/create-contest` ไปตรงๆ | หน้าต่างบังคับให้ Redirect กลับไปหน้า Login | N/A |
| TC-PEN-007 | Contest | Negative | ผู้สมัครธรรมดาพยายามแก้กติกา | เข้าระบบด้วยไอดีผู้ส่งผลงาน แล้วยิง API ลอบแก้กติกา | ระบบบล็อกการทำงาน (403 Forbidden) | N/A |
| TC-PEN-008 | User Profile | Negative | ดึงข้อมูลของทีมงานอื่น (IDOR) | ล็อกอินแอดมินกลุ่ม A แต่เปลี่ยนค่าดึงข้อมูลกลุ่ม B | ขึ้น Error ว่าไม่มีสิทธิ์เข้าถึงข้อมูลของกลุ่มอื่น | N/A |
| TC-PEN-009 | Contest | Negative | ใส่ code ข้ามไซต์ (XSS) ในชื่อการประกวด | ตั้งชื่อการประกวดเจือปนกรอบ iframe อันตราย | ระบบแสดงแค่ข้อความตัวอักษร ไม่ดึง iframe มาแสดงผล | N/A |
| TC-PEN-010 | Contest | Positive | สร้างประกวดด้วยข้อมูลครบถ้วน | กรอกชื่อ กำหนดการ และรูปภาพครบ จากนั้นกดสร้าง | กลายสภาพเป็นฉบับร่าง (Draft) ถูกบันทึกลงระบบ | N/A |
| TC-PEN-011 | Submit Poem | Negative | แก้รหัสอัปโหลดในนามคนอื่น (IDOR) | ล็อกอินเป็นคนแรก แต่แก้ไอดีตอนส่งงานเป็นคนสอง | ตีกลับหรือเซพเข้าชื่อตัวเอง ไม่ยอมส่งแทนไอดีคนอื่นได้ | N/A |
| TC-PEN-012 | Submit Poem | Negative | ส่งข้อมูลที่เยอะเกินลิมิตไปทำให้ค้าง | กรอกบทกลอน 100,000 ตัวอักษร แล้วกดส่งเข้าระบบ | ตอบกลับว่าขนาดเกิน (400 Bad Request Payload Too Large) | N/A |
| TC-PEN-013 | File Upload | Negative | อัปโหลดไฟล์หลบซ่อนไวรัส (.exe) | เปลี่ยนกลอนเป็น `.exe` หรืออัปโหลดแอปพลิเคชันไปเลย | ปฏิเสธการบันทึก แจ้งเตือนว่าห้ามใช้ไฟล์นามสกุลแปลก | N/A |
| TC-PEN-014 | File Upload | Negative | สอดไส้นามสกุลไฟล์ (MIME Bypass) | สร้างไฟล์ .sh แล้วเปลี่ยนนามสกุลหลอกเป็น .pdf ส่งแทน | ระบบจับผิด MIME พบว่าไม่ใช่เอกสารจริง จึงลบทิ้ง | N/A |
| TC-PEN-015 | Submit Poem | Positive | อัปโหลดภาพและผลงานปกติ | ส่งร้อยกรองสั้นๆ พร้อมด้วยภาพความละเอียดขนาด 1MB | อัปโหลดขึ้นระบบได้ แล้วเปลี่ยนสถานะผู้แจมเป็น 'รอตรวจ' | N/A |
| TC-PEN-016 | Grading | Negative | ให้คะแนนผู้แต่งติดลบ | กรรมการตรวจงานแล้วกรอกคะแนน -50 ลงไปบันทึก | ขึ้นให้กรอกใหม่ ระบุว่าคะแนนต้องอยู่ระหว่าง 0-100 เท่านั้น | N/A |
| TC-PEN-017 | Grading | Negative | ให้คะแนนเกินลิมิตหน้าจอ | ใส่คะแนนส่วนความสร้างสรรค์ 9999 แล้วกดเซพ | ระบบดีดกลับ แจ้งเตือนว่าค่าอยู่นอกเหนือลิมิตที่ตั้งไว้ | N/A |
| TC-PEN-018 | Grading | Negative | ยิงแก้คะแนนที่ตัดสินไปเสร็จแล้ว | กรรมการเรียกยิงส่งคำขอ HTTP ทับคะแนนผลเก่าทิ้งซ้ำ | ไม่บันทึกการแก้รอบสองเด็ดขาด หากไม่ได้อยู่ในโหมดแก้ไข | N/A |
| TC-PEN-019 | Grading | Positive | กรอกคะแนนถูกช่อง และพิมพ์คอมเมนต์ | ลงคะแนน 80 พิมพ์ 'ดีมาก' แล้วกดยืนยัน | เซพสำเร็จ ผลรวมคะแนนถูกต้อง และเปลี่ยนเป็น 'ตรวจแล้ว' | N/A |
| TC-PEN-020 | Grading | Negative | แนบโค้ดแฮ็กไว้ในคอมเมนต์ (XSS) | บรรยายพร้อมใส่รูปภาพและใส่คำสั่ง tag ปลอมไปด้วย | ไม่เรนเดอร์คำสั่ง แต่ขึ้นเป็นคำบรรยายธรรมดา หรือหายไป | N/A |
| TC-PEN-021 | Result | Negative | ดึงข้อมูลผู้ชนะก่อนวันจริง (IDOR) | ใส่เดา URL แบบตรงๆ ขอรับข้อมูลการประกวดที่ยังปิดอยู่ | ขึ้นหน้าจอสีขาว หรือแสดงแจ้งเตือน Unauthorized ป้องกันอยู่ | N/A |
| TC-PEN-022 | Result | Positive | กดประกาศอย่างเป็นทางการหน้าเว็บ | ผู้จัดตรวจสอบงาน แล้วกดปุ่มแจกผลรางวัลให้ครบวงจร | สถานะโครงการประกวดเปลี่ยน ผู้เล่นหน้าบ้านเปิดดูคะแนนได้ | N/A |
| TC-PEN-023 | Judge Area | Negative | เพิ่มอีเมลกรรมการผสม SQL | กรอก Email ลักษณะ `admin'@test.com' OR 1=1;` | จัดการฐานข้อมูลได้ปลอดภัย ไม่มีอีลลูชั่นของการซ้อนรหัส | N/A |
| TC-PEN-024 | Judge Area | Positive | แอดกรรมการที่อีเมลใช้งานได้จริง | ค้นหาไอดีเข้าสู่หน้าจีบระบบ แล้วเพิ่มสิทธิ์ | ผู้ใช้ดังกล่าวได้แถบหน้า 'งานกรรมการของฉัน' โผล่ขึ้นมาทันที | N/A |
| TC-PEN-025 | Headers | Negative | ตรวจสอบ Headers ด้านปลอดภัยของเว็บ | ใช้ tools ยิงแล้วเล็งเปิดหา Headers ซ่อน CORS, HSTS | หัวไฟล์แสดงการซ่อน Secure อย่างสมบูรณ์ ไม่ให้จิ๊กข้อมูล | N/A |
| TC-PEN-026 | Path Request | Negative | ขอดูไฟล์รหัสผ่านของเซิร์ฟเวอร์ | ดึงรูป แต่เปลี่ยนผ่าน `/uploads/../../../etc/passwd` | แทร็กเรียกไฟล์หลังบ้านไม่ได้ โดนบล็อกไม่ให้เข้าถึงเด็ดขาด | N/A |
| TC-PEN-027 | API Limit | Negative | สแปมรัวขอหน้าจอ (Rate Limiting) | เขียนสคริปต์ยิงหน้าแรกไปรัวๆ 5000 หน ในสิบวินาที | โดนจับอัตราการใช้งาน แล้วส่ง Error แจ้งเตือน Request เด้ง | N/A |
| TC-PEN-028 | Access Right | Negative | สั่งลบงานเขียนของผู้เข้าร่วมท่านอื่น | ล็อกอินไอดีแรก แต่ไปแกล้งชี้เป้าลบงานของตัวละครสอง | หลังบ้านเช็คไม่ได้ว่าเป็นเจ้าตัว จึงงดให้บริการลบงาน | N/A |
| TC-PEN-029 | Avatar Upload | Negative | สุ่มเอาไฟล์รูป SVG ปนสคริปต์ร้ายเข้าไป | ดัดแปลงโปรไฟล์ ใส่ภาพ SVG แอบแฝงจอมกวนระบบ | ไซต์ฉลาดพอที่จะหักล้างสกิลซ้อนทับภาพออก ไม่ระคายเคือง | N/A |
| TC-PEN-030 | Network | Negative | ใช้เครื่องมือดัก Packet ขณะใส่รหัสผ่าน | รอใช้ Wireshark จอดักตรงช่องทางจราจรการ Login | แพ็กเก็ตที่ได้ไปถูกเข้ารหัสด้วย HTTPS อ่านไม่ออกเด็ดขาด | N/A |

---

##  2. การใช้งาน Script ทดสอบอัตโนมัติ 


### วิธีการรัน (How to Run)
1. ติดตั้งไลบรารีเบื้องต้นสำหรับ Node.js ก่อน:
   ```bash
   cd playwright-tests
   npm install
   ```
2. ทำการรันโค้ด Playwright ทดสอบระบบ:
   ```bash
   npx playwright test
   ```
*(ผลลัพธ์การรันอาจล้มเหลว ณ ขณะนี้เนื่องจากการชี้เป้าหมายไปที่ `http://localhost:3000` รอการเปลี่ยน URL จริงเพื่อทดสอบต่อไปในอนาคต)*

###  ไฟล์สคริปต์การโจมตีที่บรรจุมาด้วย:
*ไฟล์อยู่ที่ `playwright-tests/tests/pentest.spec.js` มีกรณีดังนี้:*
- **TC-AUTO-PEN-001:** ป้องกัน SQL Injection ที่หน้าเข้าสู่ระบบ
- **TC-AUTO-PEN-002:** ป้องกันการฝัง XSS Script ลงในช่องกรอกผลงานผู้สมัคร
- **TC-AUTO-PEN-003:** ข้ามสิทธิ์ (RBAC Bypass) ทะลวงหน้าจัดประกวด
- **TC-AUTO-PEN-004:** Parameter Tampering - ให้คะแนนกรรมการเกินขีดจำกัด
- **TC-AUTO-PEN-005:** File Upload Bypass - หลบชนิดไฟล์แนบแอบยัด `.exe`

